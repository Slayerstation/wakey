# Wakey (Wake-On-LAN Application)
A cross-platform (Windows, macOS, Linux) application that is capable of sending Wake-On-LAN packets to ethernet-wired remote machines in the Local Area Network (LAN).

It has synchronous and asynchronous logging capabilities, that includes Prometheus for monitoring.

